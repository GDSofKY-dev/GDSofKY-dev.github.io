---
import { Image } from "astro:assets";
import spring_image from "../assets/images/spring.png"
import remote_image from "../assets/images/remote.png"
import custom_image from "../assets/images/custom.png"
---

<section id="services" class="services-section">
    <div class="container">
        <div class="title">
            Our Services
        </div>
        <div class="services-container">
            <div class="service-col" data-aos="fade-right" data-aos-delay="100" data-aos-anchor-placement="top-bottom">
                <div class="service-header">
                    <div class="service-icon spring-icon">
                        <i class="bi bi-tools"></i>
                    </div>
                    <h3>Spring Repair and Install</h3>
                </div>
                <p>
                    We safely repair and replace broken garage door springs with quality parts. Fast, reliable service to get your door working smoothly again.
                </p>
                <div class="service-image">
                    <Image
                    src={spring_image}
                    alt="Image of service"
                    >
                </div>
            </div>
            <div class="service-col" data-aos="fade-right" data-aos-delay="150" data-aos-anchor-placement="top-bottom">
                <div class="service-header">
                    <div class="service-icon remote-icon">
                        <i class="bi bi-broadcast"></i>
                    </div>
                    <h3>Remote and Keypad Install</h3>
                </div>
                <p>
                    From new remote installation to keypad programming, we make accessing your garage simple and secure for your family.
                </p>
                <div class="service-image">
                    <Image
                    src={remote_image}
                    alt="Image of service"
                    >
                </div>
            </div>
            <div class="service-col" data-aos="fade-right" data-aos-delay="200" data-aos-anchor-placement="top-bottom">
                <div class="service-header">
                    <div class="service-icon custom-icon">
                        <i class="bi bi-house-door"></i>
                    </div>
                    <h3>Custom Door Installs</h3>
                </div>
                <p>
                    Professional installation of standard and custom garage doors that enhance your home's value and curb appeal. We help you choose the perfect fit.
                </p>
                <div class="service-image">
                    <Image
                    src={custom_image}
                    alt="Image of service"
                    >
                </div>
            </div>
        </div>
    </div>
</section>

<style>
    .services-section {
        background-color: var(--background-50);
    }

    .title {
        font-size: 3.5rem;
        font-weight: 600;
        color: var(--text-800);
        margin-bottom: 3rem;
        text-align: left;
        line-height: 1.1;
    }

    .services-container{
        display: grid;
        grid-template-columns: repeat(3, 1fr);
        gap: 2.5rem;
        align-items: stretch;
    }

    .service-col {
        position: relative;
        display: flex;
        flex-direction: column;
        text-align: left;
        background-color: white;
        border-radius: 0.5rem;
        overflow: hidden;
        box-shadow: 0 4px 6px -1px rgba(0,0,0,0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06);
        transition: all 0.3s ease;
        border-top: 4px solid var(--primary-500);
    }

    .service-col {
        border-top-color: var(--accent-500);
        min-height: 100%;
    }

    .service-col:hover {
        transform: translateY(-4px);
        box-shadow: 0 10px 25px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2px rgba(0, 0, 0, 0.05);
    }

    .service-header {
        display: flex;
        align-items: center;
        gap: 1rem;
        margin: 1.5rem 1.5rem 1rem 1.5rem;
        padding-right: 3rem; /* Space for number badge */
    }

    .service-icon {
        width: 3rem;
        height: 3rem;
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 1.25rem;
        flex-shrink: 0;
        color: white;
        /* background-color: var(--primary-750); */
        background: linear-gradient(135deg, var(--primary-600), var(--primary-750));

    }

    .service-col h3 {
        font-size: 1.2rem;
        font-weight: 600;
        color: var(--text-800);
        margin: 0;
        line-height: 1.3;
        flex: 1;
    }

    .service-col p {
        font-size: 1.2rem;
        color: var(--text-600);
        margin: 0 1.5rem 1.5rem 1.5rem;
        line-height: 1.5;
        flex: 1;
    }

    .service-image {
        width: 100%;
        height: 250px;
        overflow: hidden;
    }

    .service-image img {
        width: 100%;
        height: 100%;
        object-fit: cover;
        display: block;
        transition: transform 0.3s ease;
    }

    .service-col:hover .service-image img {
        transform: scale(1.05);
    }

    /* .service-col:hover .service-icon {
        transform: scale(1.05);
    } */

    @media (max-width: 768px) {
        .services-container {
            grid-template-columns: 1fr;
            gap: 2rem;
        }

        .title {
            font-size: 2.5rem;
            text-align: center;
            margin-bottom: 2rem;
        }

        .service-col {
            text-align: center;
        }

        .service-header {
            flex-direction: column;
            text-align: center;
            gap: 0.75rem;
            padding-right: 1.5rem;
        }

        .service-col h3 {
            text-align: center;
        }

        .service-col p {
            text-align: center;
        }
    }
    @media (max-width: 1024px) and (min-width: 769px) {
        .services-container {
            gap: 2rem;
        }
        .title {
            font-size: 3rem;
        }
    }
</style>